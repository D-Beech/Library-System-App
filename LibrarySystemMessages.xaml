<UserControl x:Class="FinalProject.LibrarySystemMessages"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:FinalProject"
        mc:Ignorable="d"
        Style="{StaticResource WindowSizeContent}">

 
    
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width ="*"/>
        </Grid.ColumnDefinitions>

        <Grid.RowDefinitions>
            <RowDefinition Height="1*"/>
            <RowDefinition Height="1*"/>
            <RowDefinition Height="7*"/>
        </Grid.RowDefinitions>

        <Border Background="#74D3AE"  >
            <Button Style="{StaticResource InvisibleBtn}" Click="Go_Back_Click" HorizontalAlignment="Left">
                <Image Source="D:\Users\270298599\source\repos\DatuPresentationFinal\FinalProject\FinalProject\Icons\Arrow_Icon.png"/>
            </Button>
        </Border>

        <TextBlock Grid.Row="1" HorizontalAlignment="Center" FontSize="40" Foreground="White" Text="Messages Logged"/>

        <ScrollViewer Grid.Row="2" VerticalScrollBarVisibility="Hidden">
            <StackPanel Name ="MessagePanel" >
        </StackPanel>
        </ScrollViewer>

    </Grid>
</UserControl>




