<UserControl x:Class="FinalProject.Catalog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:FinalProject"
        mc:Ignorable="d"
        Style="{StaticResource WindowSizeContent}">

<!--Header-->

<Grid>
    <Grid.ColumnDefinitions>
        <ColumnDefinition Width="1*"/>
        <ColumnDefinition Width="3*"/>
        <ColumnDefinition Width=".2*"/>
        <ColumnDefinition Width=".8*"/>
    </Grid.ColumnDefinitions>

    <Grid.RowDefinitions>
        <RowDefinition Height="1*"/>
        <RowDefinition Height="0*"/>
        <RowDefinition Height=".5*"/>
        <RowDefinition Height=".5*"/>
        <RowDefinition Height="10*"/>
    </Grid.RowDefinitions>

    <Border Background="#74D3AE" Grid.RowSpan="3">
        <Button Name ="GoBackBtn" Style="{StaticResource InvisibleBtn}"  Click="Button_Click"  FontSize="30" Foreground="White" Margin="5">
            <Image Source="D:\Users\270298599\source\repos\DatuProgress\FinalProject\FinalProject\Icons\Arrow_Icon.png"/>
        </Button>
    </Border>

    <StackPanel  Background="#74D3AE"   Grid.Column="1"  Grid.ColumnSpan="2"  Grid.RowSpan="3"  >
    </StackPanel>


    <Border Background="#74D3AE" Grid.Column ="3" Grid.RowSpan="3" BorderThickness="0" Margin="0">
        <Button Name ="SignOutBtn"  Style="{StaticResource InvisibleBtn}" BorderBrush="Transparent"  Content="Sign Out" FontSize="30" Foreground="White" Click="SignOut_Button_Click"/>
    </Border>

    <Border Grid.Column="1" Grid.Row="2" Grid.RowSpan="2" Background="White" CornerRadius="50" Margin="150,4,150,4">
        <TextBox Name="searchBar" Text="Search Catalog" Tag="Search Catalog" KeyDown="Search_Entry" KeyUp="Key_Up" VerticalAlignment="Center" TextAlignment="Center" Background="Transparent" Foreground="LightGray" BorderThickness="0" FontSize="25"/>
    </Border>

    <ScrollViewer VerticalScrollBarVisibility="Hidden" Grid.Row="4" Grid.ColumnSpan="3" >
        <WrapPanel Name = "CatalogStack" Background="Transparent" Margin="20,50,0,0" />
    </ScrollViewer>

    <TextBlock Name= "SearchBlock" Grid.Column="3" Grid.Row="4" Height="400" VerticalAlignment="Top"  Margin="3,10" Foreground="Black" Background="#DD9787" TextWrapping="Wrap" FontSize="25"/>


    <!--Header-->
</Grid>

</UserControl>


